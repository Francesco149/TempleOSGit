                                Print Fmt Strs

<fmt_arg> := %[-][0][<width>][.<decimals>][<flags>][h<aux_fmt_num>]<fmt_code>

See [C:/Kernel/StrPrint.CPP.Z,208] StrPrintJoin().

<flags>:

  't' truncate to <width>.
',' add commas every three digits or four nibbles.
'$' makes %Q convert '$' to "\x24".
'/' makes %Q and %q convert '%' to "%%".

<aux_fmt_num>:

  For "%n", "%d" or "%u", the <aux_fmt_num> causes thousands mode.  "%h?n" 
will pick a var exponent multiples of three unit, while "%h-3n" will display 
milli units or "%h6n" will display mega units.  The 'k' flag is always on 
for "%n".  See [C:/Demo/Print.CPP.Z,1].

For "%c" or "%C", the <aux_fmt_num> repeats the char that many times.

<fmt_code>:

  "%n" floating point in engineering notation, exponents being multiples of 
three.  If it has a <aux_fmt> code, it will display scientific units 
letters.

"%S" [C:/Kernel/KDefine.CPP.Z,49] Define() entry.

"%C" [C:/Kernel/KernelB.HPP.Z,58] ToUpper() character.

"%h25c",'\n'; 25 new-lines.

"%h*c",25,'\n'; 25 new-lines.

"%F" text file by filename.

"%$F" [C:/Doc/DolDocOverview.TXT.Z,1] DolDoc file in memory.

"%p" ptr.

"%P" link to ptr.

"%D" date.  Pass a [C:/Kernel/KernelA.HPP.Z,185] CDate.

"%T" time.  Pass a [C:/Kernel/KernelA.HPP.Z,185] CDate.

"%z" sub_entry of an enumerated list of text entries.  See
  [C:/Kernel/StrA.CPP.Z,402] LstSub().  Pass 
sub_entry_num first, list second.

"%Z" [C:/Kernel/KDefine.CPP.Z,13] DefineLstLoad() subentry.  Pass
  sub_entry_num first, define_name 
second.

"%Q" convert "\" to "\\" and quote to backslash quote. (For use in creating 
strs in strs.)

"%q" rev a "%Q".


                                 Print Family

[C:/Kernel/StrPrint.CPP.Z,898] MStrPrint(U8 *fmt,...) is like
[C:/Kernel/StrPrint.CPP.Z,876] StrPrint(U8 *dst,U8 *fmt,...) but it returns a 
[C:/Kernel/Mem/MAllocFree.CPP.Z,387] MAllocated str.  It is vary handy
because you don't have to worry about 
overflow.

[C:/Kernel/StrPrint.CPP.Z,881] CatPrint(U8 *_dst,U8 *fmt,...) concatenates a
formated string.

[C:/Kernel/SrvCmd.CPP.Z,483] Auto(U8 *fmt,...) sends text to the current
task's input buffer.
[C:/Kernel/SrvCmd.CPP.Z,465] AutoStr(U8 *fmt,...) sends text of an
[C:/Doc/Glossary.TXT.Z,1] AutoFile to the keyboard stream of the 
current TASK but can also do mouse cmds.

[C:/Kernel/SrvCmd.CPP.Z,446] XTalk(CTask *task,U8 *fmt,...) and text to
another task's input buffer.
[C:/Kernel/SrvCmd.CPP.Z,490] XTalkStr(CTask *task,U8 *fmt,...) sends text of
an [C:/Doc/Glossary.TXT.Z,1] AutoFile to the keyboard 
stream of another TASK but can also do mouse cmds.

[C:/Adam/DolDoc/DocPutS.CPP.Z,158] DocPrint(CDoc *doc,U8 *fmt,...) sends text
to a document.  You can buffer to a 
Doc and save it, providing the functionality of fprintf.  See 
[C:/Demo/Dsk/FPrintF.CPP.Z,1].

[C:/Kernel/SrvCmd.CPP.Z,407] Adam(U8 *fmt,...) sends text to the adam task to
be compiled and run.
[C:/Kernel/SrvCmd.CPP.Z,424] AdamLog(U8 *fmt,...) and
[C:/Kernel/SrvCmd.CPP.Z,434] AdamErr(U8 *fmt,...) send text to the adam task
to be 
displayed.

[C:/Compiler/CMisc.CPP.Z,68] StreamPrint(U8 *fmt,...) sends text to the
stream of code being compiled and 
must reside in a #exe{} blk.

[C:/Adam/Gr/GrBitMap.CPP.Z,906] GrPrint(CDC *dc,I64 x,I64 y,U8 *fmt,...) and
[C:/Adam/Gr/GrBitMap.CPP.Z,915] GrVPrint() plots text in graphics 
mode.

[C:/Adam/Gr/GrTextBase.CPP.Z,321] TextPrint(CTask *task,I64 x,I64 y,I64
attr,U8 *fmt,...) plots to [TextBase Layer,1] gr.text_base.

[C:/Compiler/CMain.CPP.Z,605] ExePrint(U8 *fmt,...) compiles and execute a
string.  Note: It returns the res 
of the last executed expression.

[C:/Adam/AutoFile.CPP.Z,26] AFPrint(I64 mS,U8 *fmt,...)
[C:/Kernel/KeyDev.CPP.Z,20] PutChars()s one at a time with a delay.

[C:/Kernel/KDbg.CPP.Z,217] RawPrint(I64 mS,U8 *fmt,...) sends direct to
screen memory, bypassing window 
manager.

[C:/Kernel/KTask.CPP.Z,394] User(U8 *fmt,...) Spawns a user and execute a
string on start-up.

[C:/Kernel/SrvCmd.CPP.Z,398] PopUpPrint(U8 *fmt,...) compiles and execute a
string in a pop-up win.  Note: 
It returns the res of the last executed expression.
[C:/Adam/DolDoc/DocPopUp.CPP.Z,17] PopUpViewPrint(U8 *fmt,...) creates a
pop-up window and views text.

**Note:** Use Print("%s",src) if you need an unmodified string.
